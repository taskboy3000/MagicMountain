% for my $a (@$actions) {
  <button
%   for my $k (sort keys %{ $a->{attrs} // {} }) {
%     my $v = $a->{attrs}{$k};
%     if (defined $v) {
    <%= $k %>="<%= $v %>"
%     } else {
    <%= $k %>
%     }
%   }
  ><%= $a->{label} %></button>
% }
